/// <summary>
/// カフェ運営画面 (View) が
/// Presenter に対して公開するインターフェース
/// </summary>
public interface ICafeView
{
    // 将来のタスクで、Presenter が View を操作するためにメソッドを追加していく
    // (例: UpdateScore(int score), AddTask(TaskData task))
}
